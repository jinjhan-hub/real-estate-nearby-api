# Codex Current Task

目前任務：

```txt
V0.3 Admin Dashboard 規劃文件
```

狀態：

```txt
尚未開始
```

任務說明：

```txt
規劃 Admin Dashboard 需要顯示哪些資訊、需要哪些資料來源，但不實作 Admin API 或 Admin UI。
```

預期新增文件：

```txt
docs/admin-dashboard-plan.md
```

限制：

- 只新增規劃文件。
- 不修改 API。
- 不接 Supabase。
- 不接 Google API。
- 不修改 Vercel env。
- 不新增資料表。
- 不新增 migration。
- 不部署。

備註：

之後每次任務開始前，應先讀取本文件，以確認目前指定任務。完成 V0.3 後，再由使用者明確要求是否更新 current task 文件。
